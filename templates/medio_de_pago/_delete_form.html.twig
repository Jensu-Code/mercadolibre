<form class="float-end" method="post" action="{{ path('app_medio_de_pago_delete', {'id': medio_de_pago.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medio_de_pago.id) }}">
    <button class="btn btn-danger mt-4">Delete</button>
</form>
