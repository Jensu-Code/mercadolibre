{% extends 'base.html.twig' %}

{% block title %}MedioDePago{% endblock %}

{% block body %}
    <h4>MedioDePago</h4>

    <table class="table style-two">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ medio_de_pago.id }}</td>
            </tr> #}
            <tr>
                <th>Nombre</th>
                <td>{{ medio_de_pago.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mt-4" href="{{ path('app_medio_de_pago_index') }}">back to list</a>

    <a class="btn btn-success mt-4" href="{{ path('app_medio_de_pago_edit', {'id': medio_de_pago.id}) }}">edit</a>

    {{ include('medio_de_pago/_delete_form.html.twig') }}
{% endblock %}
