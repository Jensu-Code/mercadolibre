{% extends 'base.html.twig' %}

{% block title %}ProductoCategoria{% endblock %}

{% block body %}
    <h4>Producto Categoria</h4>

    <table class="table style-two">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto_categorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ producto_categorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mt-4" href="{{ path('app_producto_categoria_index') }}">back to list</a>

    <a class="btn btn-warning mt-4" href="{{ path('app_producto_categoria_edit', {'id': producto_categorium.id}) }}">edit</a>

    {{ include('producto_categoria/_delete_form.html.twig') }}
{% endblock %}
